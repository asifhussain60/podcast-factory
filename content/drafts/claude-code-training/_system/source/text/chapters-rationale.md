## Source chapter 1 — EP01 Source: Claude Code for Copilot Users — Understanding the Shift

`ch01-copilot-to-claude-code-mental-shift.txt` — Source chapter 1 of 3 is a 2,885-word author-pre-segmented unit covering Episode 1 of the three-part series. The author has explicitly framed it as the "why" episode in a why → what → how arc, with a discrete pedagogical purpose: the mental-model shift from autocomplete to agentic, anchored in the audience's existing Copilot competence rather than positioned against it. The chapter file expands the source faithfully to 5,791 words to land inside the Extended-tier band (5,500–9,500) without inflating the argument — the expansion is concrete walk-through prose on each section the source labels (PODCAST FORMAT GUIDANCE, mental model shift, architectural difference, Copilot agent mode, VS Code integration, capability map, pricing, the complementary-tools landing, dual-tool workflow, what's ahead), with every factual claim grounded in the source's named official documentation (code.claude.com, anthropic.com, docs.github.com) and no speculation introduced. Format is `deep_dive` because the chapter unfolds one position layer-by-layer with no named opposing voice — the student is curious-but-precise, asking pointed questions from competence, never advocating for a rival framing; the teacher respects Copilot throughout and lands on complementarity rather than displacement. Host dynamic is `curious_mind + scholar_companion` per the deep_dive mapping; essentiality is `core` because removing the architectural framing would strand Episodes 2 (the agentic mental model) and 3 (the hands-on session) without their motivating why. The chapter contributes directly to the book's central claim that Claude Code is additive-not-substitutive to a Copilot-based workflow.

## Source chapter 2 — EP02 Source: How Claude Code Actually Works — The Agentic Mental Model

`ch02-how-claude-code-actually-works.txt` — Source chapter 2 is pre-segmented by the author as Episode 2 of 3 with a single coherent purpose: build the agentic mental model that bridges the *why* (EP01's architectural framing) to the *how* (EP03's hands-on session). The source carries an eight-part architectural tour — agentic loop, tool system, CLAUDE.md and persistent memory, slash commands, Hooks as enforcement layer, MCP, permissions, cost — and these parts are mutually load-bearing for the mental model; splitting them would fragment the working architecture the listener needs to internalize before EP03's install-and-run lands as informed choice rather than as instruction-following. Authored as `deep_dive` (one position unfolding layer-by-layer; no named opposing voice — the Copilot contrast is a recurring anchor the teacher uses, not a foil being argued against), `core` essentiality (middle leg of the why/what/how arc; removing it strands EP03), and `curious_mind + scholar_companion` host dynamic (the source's own format guidance prescribes a teacher-student dialogue where the student is engaged and building understanding actively, not being converted). The source's 3,365 words were expanded to 7,675 words through faithful prose elaboration — adding the student's question hooks the source flagged as anchors ("does this scale compared to Copilot's context window?", "what stops the loop from running indefinitely?", "is Hooks configuration something most developers set up or is it advanced?"), surfacing the implicit honest-about-effort tone the source's format note demanded, and weaving the Copilot anchor through each part — comfortably inside the extended tier band (5,500–9,500). No latitude-on-omissions exercised; every architectural fact, code-block reference, and verbatim doc quotation from the source is preserved with attribution. No enrichment from outside whitelisted traditions is needed here: this is a technical-documentation source about Anthropic and GitHub tooling, and the in-text citations to `code.claude.com`, `anthropic.com/engineering`, and `docs.github.com` are the appropriate authority layer for the subject matter.

## Source chapter 3 — EP03 Source: Getting Up and Running with Claude Code — Your First Session

`ch03-first-session-walkthrough.txt` — Source chapter 3 is pre-segmented by the author as Episode 3 of 3 (the practice capstone of the why/what/how arc) with a single coherent purpose: convert the architectural framing of EP01 and the agentic mental model of EP02 into an executable action plan — install, authenticate without the ANTHROPIC_API_KEY billing trap, run a first session against the developer's own codebase, generate and refine CLAUDE.md, and absorb the ten Copilot-user friction points before they cost a week of confused adoption. The twelve practical parts (prerequisites → installation → authentication → VS Code integration → first session → day-one workflows → undoing and recovery → CLAUDE.md setup → ten gotchas → command reference → configuration → day-one checklist) are mutually load-bearing for the hands-on outcome the audience expects; splitting them would fragment the action plan and merging with neighbors would conflate the *how* with the *what* (EP02). Authored as `deep_dive` (one position unfolding layer-by-layer; no named opposing voice — the Copilot contrast is a recurring anchor the teacher uses to validate the listener's existing intuitions and redirect them accurately, not a foil being argued against; the source's own format guidance explicitly labels this a teacher-student dialogue where the teacher anticipates friction points and validates rather than dismisses), `core` essentiality (practice capstone of the three-part arc; removing it would leave the listener with a mental model but no path to practice), and `curious_mind + scholar_companion` host dynamic (the source's format note explicitly prescribes the experienced practitioner who has guided many Copilot users through the first session pairing with a senior developer whose energy is forward-moving rather than impatient — practical questions like "where do I start?" and "is this normal behavior?"). The source's 3,144 words were expanded to 8,238 words through faithful prose elaboration — surfacing the five student question hooks the source explicitly flagged as anchors (uninstall Copilot first, ANTHROPIC_API_KEY conflict, no suggestions appearing at the prompt, per-edit permission prompt as normal behavior, context-window fullness warnings), weaving the Copilot anchor through each of the twelve parts, expanding the ten-gotchas section into the chapter's highest-value beat with full validation language rather than terse bullets, and closing on the nine-step day-one checklist as the student's concrete action plan — comfortably inside the extended tier band (5,500–9,500). No latitude-on-omissions exercised; every command, file path, environment variable, code block, table, and verbatim documentation quotation from the source is preserved with attribution. No enrichment from outside whitelisted traditions is needed here: this is a technical-documentation source about Anthropic and GitHub tooling, and the in-text citations to `code.claude.com/docs/en/setup`, `code.claude.com/docs/en/authentication`, `code.claude.com/docs/en/memory`, `code.claude.com/docs/en/permission-modes`, `code.claude.com/docs/en/best-practices`, `code.claude.com/docs/en/quickstart`, and `support.anthropic.com` are the appropriate authority layer for the subject matter.

