# Reading-edition review punch list — The Master and the Disciple

Asif's review findings on the rendered PDF (started 2026-06-11). Items are
fixed in batch after the review pass completes; each fix re-renders the PDF
locally (no pipeline re-run).

| # | Status | Finding | Fix sketch |
|---|---|---|---|
| 1 | OPEN | Arabic renders in the default system font — not a proper naskh face. Asif expects Amiri, Traditional Arabic Bold, or the Quran.com face (KFGQPC Uthmanic Script HAFS) | Add @font-face + `[lang=ar]`/Arabic-block font stack in the book render theme CSS used by build_book_pdf.py's Playwright render; embed the font file so the PDF is portable |
| 2 | OPEN | Book has no cover — add a generated title-cover image as the PDF's first page | Generate one cover image via Imagen (same path the video layer uses; Gemini spend pre-authorized), editorial-modern aesthetic per the locked reader direction, save to book/cover.png; render step prepends a full-bleed cover page (image + title + author) before the TOC |
| 3 | OPEN | SVG diagram text is far smaller than body text — unreadable at reading zoom (e.g. "Mutual Subsistence" zahir/batin table) | Diagram generator (_book_illustrate) must enforce a minimum effective font size relative to rendered width — same 1.2rem reading-floor principle the site standard uses; scale SVG viewBox/typography so labels print at body-comparable size |
| 4 | OPEN | Text overwriting/collision inside diagram cells — "Standing Alone" row renders overlapping garbled text escaping the cell bounds | Root cause: SVG <text> lines with hardcoded coordinates and no wrapping for long content. Generator must wrap text to measured cell width (tspan lines) or size cells to content; add a deterministic overflow check (text length vs cell box) at generation time |
| 5 | OPEN | No visual challenger exists for the rendered book PDF — semantic book-challenger never sees pixels, so font-variance/overlap/overflow defects ship undetected | New convergence step in 0book-render: rasterize rendered pages containing figures (Playwright screenshots or pdftoppm), run a visual QA pass (LLM-vision or deterministic SVG-geometry lint) for text overlap, overflow, and minimum-type-size violations; findings loop back to the diagram generator, re-render until clean (cap iterations like other challengers) |
| 6 | OPEN | Arabic quotation blocks should be styled like a beautiful Quran: Arabic script AND English translation both centered, each in its own color, with a golden theme on Quranic verses | Theme CSS: center both lines in the quote blockquote; Arabic in the naskh face from item 1 at generous size with its own color; English translation centered in a distinct color; gold accent treatment (border/ornament/background tint) for verse blocks. Implementation note: compose output may not tag quote TYPE (Quran vs hadith vs saying) — if differentiated gold-for-Quran-only is wanted, blocks need a type class at compose/inject time; otherwise apply the treatment to all Arabic quote blocks. ALSO observed page 12: stray Latin period renders at the start of the Arabic line (RTL punctuation artifact, '.انا عبد الله') — strip trailing Latin periods from Arabic lines or set dir=rtl on the Arabic span |
| 7 | OPEN | Slide figures caption as "(untitled page)" — meaningless label under images (e.g. The Anatomy of Knowledge slide) | Caption fallback chain in inject_slide_deck/_slide_import: use the manifest slide `title` first (it exists — LLM/hand-authored), then pdftotext page title; if neither, omit the figcaption entirely. Never emit "(untitled page)" |
| 8 | OPEN | Chapter headings look like web H2s ("5. The World, the Hereafter, and the Speech of Parables") — want professional book-style chapter openings | Theme CSS + render: each chapter starts on a fresh page; small-caps eyebrow "CHAPTER FIVE" (number word, letterspaced, muted color) above the title; title in display serif, no "N." prefix; thin rule or ornament below; generous top whitespace (~1/3 page); optional drop cap on the opening paragraph. Strip the "N. " prefix at render (keep in TOC metadata). Editorial-modern restraint per locked aesthetic (Stripe/MIT Press) |
