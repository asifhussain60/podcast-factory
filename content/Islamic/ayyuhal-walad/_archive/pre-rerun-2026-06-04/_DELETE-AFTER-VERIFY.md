# Pre-rerun backup — Ayyuhal Walad (2026-06-04)

Snapshot of the **prior 3-episode run** taken immediately before re-running the
book from phase 0b through the latest pipeline (per-step). Contents: chapters/
(+ literary/), episodes/, chapter-contracts/, slide-decks/, _stages/, and the key
`_system/` artifacts (orchestrator-state, enrichment-log, unified-book/-augmented,
challenger-report, stage-metrics, augmentation-ledger).

**Safe to delete** once the fresh run is verified and you're satisfied with the new
output. Also recoverable from git (tag `backup/ayyuhal-walad-pre-rerun-2026-06-04`).

The curated multi-source bundle (`_system/source/`) was NOT reset — the rerun
re-refines the same source from phase 0b.
