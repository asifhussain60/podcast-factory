# Resume brief — single-session continuation (2026-07-16)

Paste the block below into a fresh session to resume all work on one thread.

```
Resume the podcast-factory work. Repo: /Users/asifhussain/PROJECTS/podcast-factory,
branch develop (was at 4203d7a; run `git fetch && git status` first — another session
may have added a kitab-al-riyad glossary commit on top).

WHAT THIS THREAD ACCOMPLISHED (all committed + pushed to develop):
1. Ismaili Scholar Gem backend tool (commit ead8127): lib/reader/gems/ + api/ai/
   gem-explain.ts + gem-answer.ts + gemini-server.ts opts change. Backend only — the
   Companion-panel UI was deferred ("design later").
2. v2 book-pipeline compose fixes — preface emission, sequential-window continuity,
   similarity-based seam de-dup, ship-gate coverage fix (is_faithful_translation_
   deliverable so v2-faithful books get B4/B5/B6), and 3 render-CSS P1 fixes in
   book-print.css. All merged to develop via fb0af1b.
3. v2 knob-matrix VALIDATION COMPLETE — c5 BOOK-SOUND, c6 SHIP-WITH-CAUTION, c7+c8
   SHIP-READY (all 4 combos fidelity-safe). Recorded in
   _workspace/plan/v2-matrix/CONTINUATION.md (commit 4203d7a). The v2 route is
   cutover-ready.

REPO STATE: the chore/pdf-route-consolidation branch was merged to develop and deleted
(Phase 7 done); cutover-consolidation work is already live on develop (5ffee7b routes
fiction PDF through the canonical build path). A separate session is running a glossary
Arabic-fill on kitab-al-riyad — non-conflicting, let it finish. The M&D content branch
Islamic/the-master-and-the-disciple (worktree at ../podcast-factory-worktrees/) holds
the re-validated c5 at 1b59e09; its worktree has discardable validation residue.

OPEN THREADS (pick up in this order unless I redirect):
A. Finish the Phase-5 cutover on develop — flip the v2 default ON and delete the legacy
   compose paths (generate_translation_edition.py, the legacy 0book-compose dispatch in
   phases/book_driver.py, _inject_figures in _book_illustrate.py, inject_slides in
   _slide_import.py), remove flag scaffolding, run repo-surgeon --scope podcast. Guide:
   _workspace/plan/book-pipeline-cutover.md + _workspace/plan/v2-matrix/working-plan.md
   Phase 5. This is Tier-2 — confirm before deleting.
B. Close the augment validation gap: source_only augment added 0 blocks on M&D (a
   doctrine-native source) — the veto works but its ADDITIVE path is unproven; run a
   cell on a book whose knowledge base has faithful non-redundant material.
C. M&D c5 render polish (non-blocking): 3 fixture P1s need Book Composer re-curation in
   visual-layout.json — two same-anchor wrap pairs (p10, p27) and a dense raster at 40%
   (p86, wants standalone >=60%).
D. Self-study PDF deliverable: approved render-time plan at
   _workspace/plan/self-study-deliverable-plan.md, scheduled post-cutover, with 2 open
   decisions (Key Takeaways vs the anti-summary principle; sub-heading provenance).
E. Gem Companion-panel UI (backend ready, UI deferred).

METHODOLOGY LESSON: when validating matrix cells, challenge each from an ISOLATED
snapshot (restore its book.md + stamp its config, no compose running) — never run a
challenger in parallel with the next cell's compose in the same worktree, or it judges
the wrong (overwritten) book.md. Also: git worktrees start without node_modules
(gitignored) — symlink the primary repo's node_modules in to render PDFs.

Start by fetching, confirming develop is clean, then tell me which of A-E to take.
```
