# Podcast Auditor Report — 2026-05-26T12:53:28Z

**Auditor version**: 1.1
**Scope**: core
**Since**: full history
**Run started**: 2026-05-26T12:53:28Z
**Run completed**: 2026-05-26T12:53:28Z
**Verdict**: regression-detected

## Headline summary

- P0 findings: 7
- P1 findings: 9
- P2 findings: 3
- New since last run: n/a (audit-reports/ was deleted in commit 4c92b2b; treat as first run on new substrate)

The cumulative effect of three consolidation commits (`78ce80e`, `4c92b2b`, `90fa22e`) plus the steward additions (`0e9bb7a`, `f0b0628`) is **substantial spec ↔ disk drift**. Several agent specs and Python modules still point at filenames that the consolidation deleted (`podcast-plan.yaml`, `acceptance-criteria.md`, `intelligence-pipeline-wave1-spec.md`, `podcast-plan-DoR-appendix.md`). The newly-landed `vacuum.md` §9 worked example uses bucket names (`pipeline/`, `ship-criteria/`) that differ from develop's actual layout (`refactor/`, `operations/`, `debt/`). The `podcast-auditor` spec has TWO divergent copies (`infra/claude-agents/` vs `.github/agents/`) — only the `infra/` copy has the new AU-H1 probe. And `content/drafts/` contains a parallel `BOOKS/` subtree carrying live M4A audio whose existence is not documented in the canonical specs.

## Findings by axis

### Axis A — Efficiency

| ID | Severity | Location | Title | Proposed fix |
|---|---|---|---|---|
| AU-E1-001 | P2 | `_workspace/plan/_drivers/` | Helper scripts (binder_driver.py, commit_binders.sh, image_dedupe.py, stub_sidecars.py) — no callers grep’d in scripts/podcast/ or any agent spec | Audit + document or move to `tools/` or delete |
| AU-E2-001 | P1 | `scripts/podcast/sanitize_chapter_for_tts.py` + `scripts/podcast/restructure_episode_prompt.py` | Two narrow per-file rewriters with similar invocation contract and overlapping responsibilities (chapter sanitation + episode-prompt restructure) | Merge into a single CLI with subcommands, or document role split in module docstrings |
| AU-E3-001 | P1 | em-dash / honorific / phonetic checks live in `_rules.py` + `audit_transcript.py` + `build_episode_txt.py` + `build_slide_deck.py` | Validation pattern duplication noted in standing memory + confirmed in source | Factor into a shared `_validators.py` module with one canonical implementation per check |

### Axis B — Accuracy

| ID | Severity | Location | Title | Proposed fix |
|---|---|---|---|---|
| AU-A1-001 | **P0** | `.github/agents/project-steward.agent.md:35,54,69,167` | Steward agent reads `_workspace/plan/podcast-plan.yaml` as an authority file — that file was deleted in commit 78ce80e. The agent halts on missing authority file. | Update steward to read `_workspace/plan/refactor/plan.yaml` |
| AU-A1-002 | **P0** | `infra/claude-agents/podcast-blueprint.md:57-58,73` | Reads `_workspace/plan/podcast-plan.yaml` (deleted) and `_workspace/plan/podcast-plan-DoR-appendix.md` (deleted) | Point to `_workspace/plan/refactor/plan.yaml` or remove the reference if obsolete |
| AU-A1-003 | **P0** | `infra/claude-agents/podcast-challenger.md:42,664` | `reads_guidance` includes `_workspace/plan/acceptance-criteria.md` (deleted); Category S6 references the same file | Migrate to `_workspace/plan/operations/per-book-ship-checklist.md` or remove |
| AU-A1-004 | **P0** | `scripts/podcast/_acceptance.py:20`, `scripts/podcast/run_wave.py:37,357`, `scripts/podcast/tests/test_run_wave.py:5,26,196`, `scripts/podcast/phases/__init__.py:12,15` | Python modules + their tests reference `_workspace/plan/acceptance-criteria.md` (deleted). `DEFAULT_ACCEPTANCE_FILE` path resolves to a non-existent file; `run_wave.py` will fail when invoked. | Either restore the file at the new canonical path (`_workspace/plan/operations/`) or update modules + tests + remove the run_wave acceptance scan |
| AU-A1-005 | **P0** | `scripts/podcast/knowledge/extractor.py:7,54,61`, `knowledge/librarian.py:9,61,68`, `knowledge/augmenter.py:11,64`, `knowledge/__init__.py:9`, `knowledge/_atom_schemas.py:5` | All four knowledge-pipeline modules cite `_workspace/plan/intelligence-pipeline-wave1-spec.md` as their authoritative spec — the file was deleted. README of `_workspace/plan/` says contents were folded into `architecture.md` §Intelligence Layer + `refactor/plan.md` Wave B. | Update spec references to the new authoritative locations |
| AU-A1-006 | **P0** | `skills-staging/repo-surgeon/skill.md:27,310,313,396-406` | Pass 5 L7 + L10 reference `_workspace/plan/podcast-plan.yaml` and `_workspace/plan/acceptance-criteria.md` — both deleted | Rewrite L7+L10 against `refactor/plan.yaml` + `operations/per-book-ship-checklist.md` |
| AU-A1-007 | P1 | `infra/claude-agents/vacuum.md:246-249,263-271,289-294` | §9 worked-example mapping table proposes subfolders `pipeline/`, `ship-criteria/`, `podcast-reader/` — develop's canonical layout uses `refactor/`, `operations/`, `reader/`. The KEEP list also includes `pipeline-refactor.md`, `pipeline-refactor.yaml`, `acceptance-criteria.md`, `podcast-plan.yaml`, `podcast-reader-polish-*.md` — files that don't exist (renamed/deleted in commit 78ce80e). | Rewrite §9 worked example + KEEP list against the canonical layout (`architecture.md`, `refactor/plan.{md,yaml}`, `conventions/`, `debt/pipeline-debt.md`, `operations/per-book-ship-checklist.md`, `reader/polish-and-ai.md`, `postprod-vacuum/`). This is the LOAD-BEARING new spec for AU-H1 — its worked example must be aligned. |
| AU-A1-008 | P1 | `_workspace/README.md:26` | Claims `audit-reports/` was deleted as part of the workspace cleanup; the auditor spec REQUIRES the folder to exist at `_workspace/audit-reports/`. Also claims `chats/` was deleted; an untracked `_workspace/chats/chat01.md` already exists. Also claims `audit/` and `logs/` are gitignored — `audit/` still has 34 tracked files under `kitab-al-riyad/`. | Update README to reflect the real state OR delete the lingering files |
| AU-A1-009 | P1 | `.github/agents/podcast-auditor.agent.md` vs `infra/claude-agents/podcast-auditor.md` | Two divergent copies of the auditor spec. Only `infra/` has the AU-H1 hygiene probe (Axis E). The `.github/` copy still says "12 probes (4 axes × 3 each)". Both stamp `auditor_version: "1.1"`. | Decide canonical home (recommend `infra/claude-agents/`), delete the duplicate or convert it to a thin pointer |
| AU-A2-001 | P1 | Whole `.github/agents/` vs `infra/claude-agents/` tree | Eight specs are duplicated across both directories (`docs-updater`, `podcast-auditor`, `podcast-blueprint`, `podcast-challenger`, `podcast-extract`, `podcast-orchestrator`, `podcast-publisher`, `podcast-trainer`, `refine-prompt`, `slide-deck-challenger`). Five specs live in ONLY ONE of the two trees (`postprod-review` + `vacuum` only in `infra/`; `CORTEX`, `operating-contract`, `podcast-librarian`, `project-steward`, `reconcile`, `repo-surgeon` only in `.github/`). | Pick one canonical location (`infra/claude-agents/` is consistent with the auditor's own `reads_guidance`), migrate, leave breadcrumbs |
| AU-A3-001 | P1 | `content/drafts/BOOKS/` parallel subtree | A second drafts layout exists at `content/drafts/BOOKS/{kitab-al-riyad,islr-mas-i,the-master-and-the-disciple}/` carrying live M4A audio + slide-decks for KaR + `m4a/v1/` legacy mp3s for M&D. The canonical layout per all agent specs is `content/drafts/<slug>/` (flat). The duplicate path appears only in `architecture.md` diagram labels + `refactor/plan.md:260` (Rasāʾil intake recommendation). Active books have orchestrator-state.json under the flat path; audio lives under the nested path. | Reconcile to one canonical layout. If `content/drafts/BOOKS/<slug>/` is the target, update all specs; if flat is the target, move M4As back into per-book `m4a/` siblings. This is THE single largest source of drift in the repo today. |

### Axis C — Scalability

| ID | Severity | Location | Title | Proposed fix |
|---|---|---|---|---|
| AU-S1-001 | P2 | `scripts/podcast/_rules.py` | Several uppercase constants (`CHALLENGER_VERSION`, `SLIDE_DECK_CHALLENGER_VERSION`, `ALLOWED_CATEGORIES`, `LEARNING_DIR`) accept the current convention; no new magic-number leakage detected post-merge | None — existing pattern adequate |
| AU-S2-001 | P0 | (clean) | No new `/Users/` or `/home/` hardcoded paths landed in this merge chain | None |
| AU-S3-001 | P1 | Recent agent additions (`vacuum.md`, `postprod-review.md`, `project-steward.agent.md`) | None of the new agent specs document a cost-cap contribution to `orchestrator-state.json` cost dict. Vacuum is mutation-only (no LLM spend) — clean. Postprod-review reads transcripts with no LLM call — clean. Steward composes existing agents — inherits theirs. | Document in each agent spec that "this agent does not consume LLM tokens directly" so the cost-cap surface stays auditable |

### Axis D — Extensibility

| ID | Severity | Location | Title | Proposed fix |
|---|---|---|---|---|
| AU-X1-001 | P1 | Branch-prefix map | `_branching.py` correctly owns the category → prefix mapping (`books/`, `doc/`, etc.) — registry pattern is in place. No drift detected. | None |
| AU-X2-001 | P1 | Agent ecosystem | Nine agents now collaborate but there's no shared `BaseAgent` contract — every spec re-declares its own `agent_contract:`/`vacuum_contract:`/`postprod_contract:` frontmatter shape independently. The auditor's own `auditor_contract:` is differently-shaped again. | Consider a shared agent-contract schema (YAML JSONSchema) once a 10th agent lands. Not urgent. |
| AU-X3-001 | **P0** | Filename convention conflict | The standing rule "no version markers in filenames" (T17 in postprod-vacuum-tasks.md) is violated by `content/drafts/the-master-and-the-disciple/audits/v2.2-vs-v2.1-diff.md`. The refactor plan (`refactor/plan.md:100`) explicitly flags it for removal/rename. Also: `framework.md:3` carries `**Version:** 4.1` — direct DR-009 violation. | Delete the v2.2-vs-v2.1 diff artifact (it's a one-shot comparison better done via `git diff`); strip the `Version:` stamp from `framework.md`. Install the pre-commit hook from `refactor/plan.md` step A2. |
| AU-X3-002 | P2 | `_workspace/plan/postprod-vacuum/` vs sibling buckets | Naming convention: most plan buckets use single-word lowercase (`conventions/`, `debt/`, `operations/`, `reader/`, `refactor/`, `research/`, `view/`), but `postprod-vacuum/` is hyphenated. Acceptable per content (multi-word agent name) but inconsistent. | Acceptable as-is; flag in case a third hyphenated bucket joins |

### Axis E — Hygiene

| ID | Severity | Location | Title | Proposed fix |
|---|---|---|---|---|
| AU-H1-001 | **P0** | repo-root `.DS_Store` + 20 other `.DS_Store` files across `CONTENT/` and `scripts/` | Not in vacuum §9 root-legit whitelist. The case-insensitive Mac filesystem is also surfacing `CONTENT/` (uppercase) as a parallel view of `content/` — see refactor/plan.md A2 for the pre-existing investigation note. | `find . -name .DS_Store -delete` + extend `.gitignore` if not already covered. The auditor's spec mandates this kind of root cleanup; vacuum's `--workspace-root --apply` is the authorized fixer. |
| AU-H1-002 | **P0** | `content/drafts/BOOKS/kitab-al-riyad/episodes/podcast-workspace.code-workspace` | Orphan `.code-workspace` file inside a book's `episodes/` folder; not in canonical layout per `vacuum.md` §2. | Move to `_workspace/runbooks/` (where `podcast-factory.code-workspace` already lives) or delete |
| AU-H1-003 | P1 | `_workspace/audit-reports/` was missing entirely before this run | The auditor spec requires the folder; the workspace-cleanup commit deleted it without re-creating. This run recreated it via `mkdir -p`. | Decide whether `_workspace/audit-reports/` should be gitignored (like `audit/` + `logs/`) or tracked. Update `_workspace/README.md` accordingly. |
| AU-H1-004 | P1 | `_workspace/chats/chat01.md` (untracked) | A `chats/` subdir reappeared after the `4c92b2b` cleanup deleted it. Per `vacuum.md` §9 universal "subfolder by default" rule, single-file `chats/` is borderline acceptable but the `_workspace/README.md` says chats/ is gone. | Either: track + document, or add to `.gitignore`, or move to `_archive/2026-05-26/` |
| AU-H1-005 | P1 | `_workspace/audit/kitab-al-riyad/` (34 tracked log files) | `_workspace/README.md` says `audit/` is gitignored — but `git ls-files` returns nothing while files exist on disk. README claim vs reality mismatch. | Either gitignore is working as documented (in which case verify no git tracked content) or the files need to move to `_archive/` per AU-H1 universal rule |
| AU-H1-006 | P2 | `_workspace/plan/numeric-symbolic-disambiguation-plan.md` at plan-root | The README's last row admits this file "stays at root until the code refs migrate (refactor step A4)" — a documented exception, but per the universal subfolder rule it's sprawl until A4 lands. | Acceptable for now per refactor step A4 deferral; flag for re-check after A4 lands |

## Detail

### AU-A1-001 — Steward halts on missing authority file (P0)

**Citation:** `.github/agents/project-steward.agent.md:35,54,69,167`
**VERIFIED.** The steward's spec body says: *"If any of these is missing, halt with a clear error. Do not infer."* and lists `_workspace/plan/podcast-plan.yaml` as one of the five mandatory authority files. That file was deleted in commit `78ce80e`. Running `/steward roadmap` (or any scope) today will halt before doing any work.

**Why it matters:** The newly-landed steward is the most strategically important agent in this merge wave. It is currently non-functional because of a 5-character path mismatch.

**Proposed fix (5-line diff):**
```
- `_workspace/plan/podcast-plan.yaml` — active wave, deferred work, what's
+ `_workspace/plan/refactor/plan.yaml` — active wave, deferred work, what's
```
…in four locations. Then re-verify steward's "four-pass protocol" still parses the new file shape.

**safe_to_auto_fix:** true (path rename only; semantics preserved if `refactor/plan.yaml` carries the same `phases[]` / `open_questions[]` / `risks[]` structure).

### AU-A1-004 — Live Python code points at deleted plan file (P0)

**Citation:** `scripts/podcast/_acceptance.py:20`, `scripts/podcast/run_wave.py:37,357`, `scripts/podcast/phases/__init__.py:12,15`, plus three test files.

**VERIFIED.** `_acceptance.py` defines `DEFAULT_ACCEPTANCE_FILE = REPO_ROOT / "_workspace" / "plan" / "acceptance-criteria.md"`. That path no longer exists. Anything that imports `_acceptance` or invokes `run_wave.py` will fail at file-open time. The tests under `scripts/podcast/tests/test_run_wave.py` likely synthesize their own fixture, but the production path is broken.

**Why it matters:** This is shipped Python code that depends on planning artifacts the consolidation deleted. The orchestrator's `--report` pathway and acceptance gates G1–G7 cited by `validate_ship_ready.py` may transitively rely on this surface.

**Proposed fix:** Restore `acceptance-criteria.md` under `_workspace/plan/operations/` (where `per-book-ship-checklist.md` now lives) OR delete `run_wave.py` + `_acceptance.py` if their function is fully subsumed by the per-book ship checklist. Decide explicitly — the deletion left the Python orphaned.

**safe_to_auto_fix:** false (semantic decision required: keep or retire).

### AU-A1-007 — Vacuum §9 worked example references wrong bucket names + missing files (P1)

**Citation:** `infra/claude-agents/vacuum.md:246-271`.

**VERIFIED.** The §9 worked-example table proposes destinations `pipeline/`, `ship-criteria/`, `podcast-reader/`. The actual layout uses `refactor/`, `operations/`, `reader/`. The KEEP list itemizes files (`pipeline-refactor.md`, `pipeline-refactor.yaml`, `acceptance-criteria.md`, `podcast-plan.yaml`, `podcast-reader-polish-*.md`) that don't exist on disk — they were renamed (`plan.md`/`plan.yaml`) or deleted (`acceptance-criteria.md`, `podcast-plan.yaml`, `podcast-reader-polish-*.md`).

**Why it matters:** Vacuum is the load-bearing new mutator agent. AU-H1 (the auditor's new probe) depends on `vacuum.md` §9 as the canonical whitelist. If vacuum's own worked example is misaligned with reality, every AU-H1 finding cites a moving target.

**Proposed fix:** Rewrite §9 to use the actual buckets and actual filenames. Specifically:
- Worked-example table → `refactor/plan.md`, `refactor/plan.yaml`, `operations/per-book-ship-checklist.md`, `reader/polish-and-ai.md`, `postprod-vacuum/postprod-vacuum-tasks.md`, `postprod-vacuum/postprod-vacuum-review.md`.
- KEEP list → drop the dead filenames; the bucket-index pattern in `_workspace/plan/README.md` is now the canonical surface — vacuum should reference the README rather than restate filenames.
- Add a remark that `numeric-symbolic-disambiguation-plan.md` is the one documented root exception pending refactor step A4.

**safe_to_auto_fix:** false (spec edit; benefits from author judgment).

### AU-A3-001 — Parallel `content/drafts/BOOKS/` subtree (P1)

**Citation:** `content/drafts/BOOKS/{kitab-al-riyad,islr-mas-i,the-master-and-the-disciple}/`.

**VERIFIED.** Three books have a nested `BOOKS/<slug>/` directory carrying live M4A audio (KaR has 7 m4a files, M&D has 6 m4a + 7 v1 legacy mp3s, ISLR has chapters/). All canonical `_system/orchestrator-state.json` files live in the FLAT path (`content/drafts/<slug>/_system/...`). Specs uniformly refer to the flat path; only `architecture.md` storage-tier diagram labels + `refactor/plan.md:260` (Rasāʾil intake recommendation) reference the nested form.

**Why it matters:** Postprod-review and vacuum need to read the audio files. The vacuum spec says `content/drafts/<slug>/m4a/` — but for these three books, the audio is at `content/drafts/BOOKS/<slug>/m4a/`. Running postprod-review against the M&D branch today would find an empty `m4a/` directory at the canonical path and surface false PR-PAIRING findings.

**Proposed fix:** Decide canonical layout. If flat (recommended), move:
```
content/drafts/BOOKS/<slug>/m4a/        → content/drafts/<slug>/m4a/
content/drafts/BOOKS/<slug>/_system/    → merge into content/drafts/<slug>/_system/
content/drafts/BOOKS/<slug>/framings/   → content/drafts/<slug>/framings/
```
Then delete `content/drafts/BOOKS/`. If nested is the target, update every agent spec — much more work.

**safe_to_auto_fix:** false (destructive moves across non-trivial structure; user decision required).

### AU-X3-001 — `Version:` stamps + `v2.2-vs-v2.1` filename violate DR-009 (P0)

**Citation:** `framework.md:3`, `content/drafts/the-master-and-the-disciple/audits/v2.2-vs-v2.1-diff.md`.

**VERIFIED.** DR-009 in `architecture.md:544` reads: *"No version stamps in tracked files. The git history IS the version log. Pre-commit hook rejects `Version: \d` and `*v[0-9]*.md`."* Two violations:
- `framework.md:3` — `**Version:** 4.1 (2026-05-25 cleanup wave …)`
- `v2.2-vs-v2.1-diff.md` — version-stamped filename; refactor/plan.md A2 already flags it.

**Why it matters:** The standing rule was locked just before this audit, and the very first probe finds it violated in two places. The pre-commit hook proposed in refactor step A2 doesn't exist yet — that's why these slipped in.

**Proposed fix:** (a) strip the `Version:` line from `framework.md`; (b) delete the v2.2-vs-v2.1 diff (it's a frozen comparison better done via `git diff <ref1> <ref2> -- <file>`); (c) install the `infra/git-hooks/pre-commit` hook from refactor step A2 *before* the next merge.

**safe_to_auto_fix:** true for (a) and (b); (c) requires authorization for the hook install.

### AU-H1-001 — Repo-wide `.DS_Store` sprawl (P0)

**Citation:** 20 `.DS_Store` files across `./.DS_Store`, `CONTENT/...`, `scripts/.DS_Store`, `src/.DS_Store`.

**VERIFIED.** `find . -name .DS_Store -not -path "./.git/*"` returns 20 hits. None are in vacuum §9's root-legit whitelist. The case-insensitive Mac filesystem is also surfacing `CONTENT/` as an apparent parallel of `content/` — this matches the investigation note already in `refactor/plan.md:100`.

**Why it matters:** `.DS_Store` files are explicit destructive-confirm deletes in vacuum's §2. They're polluting `git status` cleanliness checks and likely some are committed (would need `git ls-files | grep DS_Store` to confirm).

**Proposed fix:**
```
find . -name .DS_Store -not -path "./.git/*" -delete
echo '.DS_Store' >> .gitignore   # if not present
git rm --cached <any tracked .DS_Store>
```
Then investigate the `CONTENT/` vs `content/` case duplication separately per refactor step A2.

**safe_to_auto_fix:** true (destructive but mechanical; vacuum's §2 already authorizes it under `--apply`).

### AU-H1-002 — Orphan `.code-workspace` inside book episodes folder (P0)

**Citation:** `content/drafts/BOOKS/kitab-al-riyad/episodes/podcast-workspace.code-workspace`.

**VERIFIED.** A VSCode workspace file inside a book's episode-text folder. Not in any canonical layout. Sibling of `EP*.txt` files.

**Why it matters:** The `podcast-publisher` agent copies `episodes/` wholesale into `content/published/`; this orphan would propagate to the published catalog on next ship of KaR.

**Proposed fix:** Move to `_workspace/runbooks/` next to the existing `podcast-factory.code-workspace`, or delete (it's a one-shot file).

**safe_to_auto_fix:** false (destination decision needed).

## Cross-cohort patterns

**Pattern 1 — Spec/code references to the pre-consolidation plan filenames.** Findings AU-A1-001 through AU-A1-006 all share the same root cause: commit `78ce80e` deleted `podcast-plan.yaml`, `acceptance-criteria.md`, `intelligence-pipeline-wave1-spec.md`, `podcast-plan-DoR-appendix.md` without doing the docs-sweep called out in CLAUDE.md's "Post-merge holistic audit" rule. The consolidation README (`_workspace/plan/README.md`) explicitly acknowledges this at the end: *"references in active specs (SKILL.md, podcast-blueprint.md, podcast-challenger.md, repo-surgeon/skill.md) are dangling and will be updated in refactor step A2/A4 docs-sweep."* The auditor confirms: at least 6 specs + 4 Python modules + 3 tests reference deleted files. Higher-leverage fix: bundle the docs-sweep into the next merge (refactor step A2/A4) rather than continue accruing references.

**Pattern 2 — Two parallel "canonical" directory layouts.** The repo carries two competing claims: (a) the flat `content/drafts/<slug>/` layout that every agent spec uses, and (b) the nested `content/drafts/BOOKS/<slug>/` layout that hosts the M4A audio + slide-decks for active books. The architecture diagram references the nested form; everything else references the flat form. AU-A3-001 surfaces this as the single largest source of agent-vs-disk drift. Higher-leverage fix: pick one layout, do one reconcile move, then never re-litigate.

**Pattern 3 — Two parallel agent-spec trees.** `.github/agents/*.agent.md` and `infra/claude-agents/*.md` are partially overlapping, partially divergent (AU-A2-001). The `.github/` copy of `podcast-auditor.agent.md` is missing the AU-H1 probe that just landed. This is itself a form of registry-vs-disk drift at the agent-spec layer. Higher-leverage fix: pick canonical home (`infra/claude-agents/` matches the auditor's own `reads_guidance`); make `.github/agents/` a stub directory or symlink set.

## What's next

Ranked by impact × effort. Top item is highest leverage.

1. **Fix steward + blueprint authority-file paths** (AU-A1-001, AU-A1-002). Four-line edit per spec; restores the just-landed steward to functional state.
2. **Decide canonical drafts layout + reconcile** (AU-A3-001). One halt-point user decision, then mechanical moves. Unblocks postprod-review on M&D.
3. **Rewrite `vacuum.md` §9** (AU-A1-007). The new AU-H1 probe is only as useful as the whitelist it cites; aligning §9 with the actual `_workspace/plan/` layout is prerequisite to running vacuum's pre-flight check confidently.
4. **Run docs-sweep for all `acceptance-criteria.md` / `podcast-plan.yaml` / `intelligence-pipeline-wave1-spec.md` references** (AU-A1-003 through AU-A1-006). This is the refactor step A2/A4 work already pending; pull it forward.
5. **Install the DR-009 pre-commit hook + clean the two violations** (AU-X3-001). Prevents future version-stamp creep; closes the loop on the rule that was just locked.
