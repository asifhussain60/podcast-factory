# Podcast Auditor Report — 2026-05-23T20:49:40Z

**Verdict**: regression-detected

**Auditor version**: 1.0
**Scope**: core
**Since**: full history
**Run started**: 2026-05-23T20:45:01Z
**Run completed**: 2026-05-23T20:49:40Z

## Headline summary

- P0 findings: 6
- P1 findings: 10
- P2 findings: 3
- New since last run: n/a (first run)

The codebase is functional but accruing structural drift faster than it is reconciled. The clearest signal: the agent-spec mirror system documented in [scripts/podcast/sync-agent-wrappers.sh](scripts/podcast/sync-agent-wrappers.sh) has silently broken — four new agents in `infra/claude-agents/` (including this auditor, `slide-deck-challenger`, `podcast-challenger`, `podcast-extract`) have no `.github/agents/<name>.agent.md` counterpart, and two of the existing pairs have diverged. Combine that with ~25 dead-or-missing path references in `_workspace/plan/podcast-plan.yaml` + `_workspace/plan/acceptance-criteria.md` (pre-split planning docs whose paths no longer exist), a 4-way duplication of the `ALLOWED_CATEGORIES` enum, and a confirmed cost-leakage in the slide-deck path (Phase 0g costs never write to `cost-ledger.jsonl` or the orchestrator-state `cost` dict), and the system is in regression-detected territory rather than mere drift.

## Findings by axis

### Axis A — Efficiency

| ID | Severity | Location | Title | Proposed fix |
|---|---|---|---|---|
| AU-E1-001 | P1 | scripts/podcast/sanitize_contract_yaml.py | Dead script — no callers in any agent, skill, or pipeline | Delete or add a `# Invoked manually during Phase 0d YAML rescue — see <runbook>` docstring header |
| AU-E1-002 | P1 | scripts/podcast/import_transcript.py | Dead script — only referenced in its own docstring | Delete or document its role in the bring-your-own-transcript flow |
| AU-E1-003 | P1 | scripts/podcast/kar_renumber_to_sequential.py | One-shot rename script with comment "one-shot KaR rename" — outlived its purpose | Move to `_workspace/runbooks/` or delete; comment explicitly says one-shot |
| AU-E1-004 | P1 | scripts/podcast/normalize_double_parens.py | Only referenced from `_workspace/chats/` (informal note, not pipeline) | Inline into Phase 0b if still needed; otherwise delete |
| AU-E2-001 | P1 | scripts/podcast/{sanitize_chapter_for_tts.py, _tts_sanitize.py, restructure_episode_prompt.py} | TTS-sanitize trio with overlapping rules + cross-file note about HARD_CONSTRAINTS living inline in one but rules in another | Consolidate: either fully centralize in `_tts_sanitize.py` and have both consumers import, or document the split with an architecture comment that explains why HARD_CONSTRAINTS is inline-only |
| AU-E3-001 | P1 | scripts/podcast/build_episode_txt.py (META_PROSE_TELLS) + scripts/podcast/extract_chapter.py (CONTRACT_META_PROSE_TELLS) + scripts/podcast/build_slide_deck.py (imports from build_episode_txt) | Meta-prose detection lives in 2 separate lists with different shapes | Factor to `_rules.py` alongside the other rule lists; extract_chapter's CONTRACT_META_PROSE_TELLS is a near-superset |

### Axis B — Accuracy

| ID | Severity | Location | Title | Proposed fix |
|---|---|---|---|---|
| AU-A1-001 | P0 | _workspace/plan/podcast-plan.yaml + acceptance-criteria.md | ~25 references to deleted/renamed scripts (`_blueprint.py`, `_content_range.py`, `_text_analytics.py`, `_isolation_check.py`, `_learning_store.py`, `_transcript_writer.py`, `adapters/_azure_client.py`, `adapters/_manifest.py`, `blueprint_book.py`, `series_plan.py`, `sweep_imam_doctrine.py`, `test_phase_prompts.py`, `tests/api/test_mutations.py`, `tests/e2e/test_learning_loop.py`, `tests/e2e/test_refinement_determinism.py`, `tests/test_text_analytics.py`, `docs/podcast/azure-setup.md`, `docs/podcast/operator-review-gate.md`, `docs/podcast/service-startup.md`, `docs/podcast/source-adapter-registry.md`, `_workspace/plan/checkpoints/asaas-0b.md`) — none of these paths exist on disk | Either restore the documents as historical-pre-split context with a `[ARCHIVED — pre-2026-05-22 split]` banner, or excise the dead references (most appear to be from an earlier planning iteration that did not survive the redesign) |
| AU-A1-002 | P0 | infra/claude-agents/podcast-operator.md:128, :143 | References `_workspace/plan/coordination-protocol.md` which doesn't exist; canonical file is `_workspace/plan/operators/coordination-protocol.md` | s/`_workspace/plan/coordination-protocol.md`/`_workspace/plan/operators/coordination-protocol.md`/ |
| AU-A1-003 | P0 | infra/claude-agents/podcast-challenger.md:615 (changelog text) + skills-staging/clean-commit/references/folder-rules.md | References `content/podcast/.skill/registry.md` and `content/podcast/_handbook/registry.md` — neither path exists (handbook moved to `content/podcast/.skill/handbook/` and registry is at `content/drafts/<slug>/_system/registry.md`) | Annotate the changelog entry as historical and link the live per-book registry location; fix folder-rules.md to point to the current path |
| AU-A1-004 | P0 | _workspace/plan/handoff-kar-archetype-pivot.md | References `content/drafts/kitab-al-riyad/_system/orchestrator-state.json` and `…/EP10-…/00-framing.md` — these paths reflect the pre-Phase-9.5 library layout; actual in-progress book lives at `content/drafts/kitab-al-riyad/_system/` | Update the handoff doc to point at `content/drafts/<slug>/` (current canonical per CLAUDE.md §"Podcast pipeline") |
| AU-A2-001 | P1 | scripts/podcast/_rules.py:31 (`CHALLENGER_VERSION = "2.0"`) vs infra/claude-agents/podcast-challenger.md frontmatter (no `challenger_version` key in frontmatter, only inline references like "v1.5" in changelog) | The spec frontmatter does NOT declare a `challenger_version` — the only authoritative version is `_rules.py:CHALLENGER_VERSION`. But the auditor spec at line 113 says "`auditor_contract.challenger_version` in `podcast-challenger.md`" — that key doesn't exist | Either add `challenger_contract.challenger_version: "2.0"` to the agent frontmatter (mirror `_rules.py`), or update the auditor spec to acknowledge that the version lives only in `_rules.py` and is referenced (not duplicated) by the agent |
| AU-A2-002 | P1 | scripts/podcast/_rules.py:38 (`SLIDE_DECK_CHALLENGER_VERSION = "1.0"`) vs infra/claude-agents/slide-deck-challenger.md:289 (`"challenger_version": "1.0"`) | Currently in sync at "1.0" but no programmatic linkage — the agent spec hard-codes the literal "1.0" in a sample JSON ledger record rather than referencing `_rules.py` | Acceptable today (both at 1.0) but flag: when the slide-deck challenger bumps version, both must move together. Recommendation: add a comment in the agent spec pointing to `_rules.py` as the source of truth, mirroring the audio challenger's contract |
| AU-A2-003 | P2 | scripts/podcast/_progress.py:53 (`ORCHESTRATOR_VERSION = "1.2"`) | No corresponding frontmatter version in `infra/claude-agents/podcast-orchestrator.md`; the agent spec describes "v2 reconciled cap" in prose but has no version key | Add `orchestrator_version` to the agent frontmatter pointing at `_progress.py:ORCHESTRATOR_VERSION` for symmetry with the auditor's version stamping pattern |
| AU-A3-001 | P1 | content/drafts/the-master-and-the-disciple/ | No `_system/` directory; chapter files use non-conforming `Ch-NN-Title - Refined.md` naming (mixing kebab-case + spaces + .md) vs. the established `chNN-<slug>.txt` convention used by kitab-al-riyad and ayyuhal-walad | Either run `scripts/podcast/scaffold_book.py` to bring this book into pipeline shape, or annotate it as a pre-pipeline staging directory and rename to match convention before phase-0a |
| AU-A3-002 | P2 | content/drafts/asaas-al-taveel/_system/registry.md | Registry has 6 episode rows but no chapter files in `chapters/` (book is at phase 0b halted-for-transcript-review; chapters not yet built) | Not a defect (consistent with phase state). Flag only to confirm: the orchestrator should reconcile registry-vs-chapters at phase 0g, not earlier |

### Axis C — Scalability

| ID | Severity | Location | Title | Proposed fix |
|---|---|---|---|---|
| AU-S1-001 | P1 | scripts/podcast/build_episode_txt.py:102-118 + scripts/podcast/build_slide_deck.py:71-79 | Word-count constants (`CHAPTER_WORD_MIN_HARD = 500`, `WORD_COUNT_FLOOR = 1500`, etc.) and ratio constants live inline in two separate build modules. The audit spec calls out this exact pattern as "looks like config but isn't" | Factor to `scripts/podcast/_config.py` (or extend `_rules.py`) with `CHAPTER_WORD_BANDS` / `SLIDE_DECK_WORD_BANDS` constants; both consumers import |
| AU-S1-002 | P1 | scripts/podcast/_convergence.py:45 (`MAX_OUTER_ITERATIONS = 3`) + scripts/podcast/_slide_convergence.py:77 (`DEFAULT_MAX_ITERATIONS = 5`) | Magic iteration caps that the orchestrator spec describes as "v2 reconciled cap" — the value is load-bearing for the cost-cap calculation ($50 USD per chapter) | Factor to `_config.py` so the cost-cap calculation in `run_wave.py` and these caps can be reasoned about as a unit |
| AU-S1-003 | P1 | scripts/podcast/run_wave.py:57 (`COST_CAP_HARD_DEFAULT = 50.0`) | The literal "$50" appears in `orchestrator-state.json` validation, in the agent spec ("fits the $50 cost cap"), in the convergence comment, and inline in `_convergence.py` reasoning, but the constant lives in run_wave.py alone | Move to `_config.py` and import; agent spec should reference `COST_CAP_HARD_DEFAULT` rather than the literal "$50" |
| AU-S2-001 | P0 | infra/claude-agents/podcast-extract.md:43 | Hard-coded absolute path `/Users/asifhussain/PROJECTS/podcast-factory/worktrees/main/scripts/podcast/extract_chapter.py` — breaks any other machine, even Asif's other worktree | Replace with `scripts/podcast/extract_chapter.py` (relative from repo root, consistent with every other agent spec) |
| AU-S2-002 | P0 | infra/claude-agents/refine-prompt.md:28-34 | Six `/Users/asifhussain/PROJECTS/podcast-factory/worktrees/main/...` absolute paths in the orientation checklist | Replace with relative paths; the agent runs from repo root and shouldn't require Asif's hostname structure |
| AU-S3-001 | P0 | scripts/podcast/build_slide_deck.py + scripts/podcast/_slide_authoring.py + scripts/podcast/_slide_convergence.py | The entire slide-deck phase (0g) emits ZERO cost-ledger entries. `grep -n cost` returns one comment-only hit ("`# Outer (per book) : orchestrate_book.py time / cost cap`"). `cost-ledger.jsonl` for KaR contains 0 slide-deck rows. The orchestrator-state.json `cost` dict has `{"azure_usd": 0.0, "anthropic_usd": 0.0}` and no slide-deck slot | Add `from _cost_ledger import append_cost_row, parse_usage_from_stdout` to the slide-deck convergence path; emit one row per challenger invocation. Add `slide_deck_usd` to the `cost` dict schema in `_progress.py`. Without this, the $50 cost cap is not enforced on slide-deck spend |

### Axis D — Extensibility

| ID | Severity | Location | Title | Proposed fix |
|---|---|---|---|---|
| AU-X1-001 | P1 | scripts/podcast/orchestrate_book.py:106, scaffold_book.py:39, ingest_source.py:71, audit_page_markers.py:222 | `ALLOWED_CATEGORIES` literal `{books, articles, documents, lectures, interviews, letters}` duplicated across 4 files (as `tuple` in one, `set` in two, inline `choices=[...]` in the fourth) | Define `ALLOWED_CATEGORIES` once in `_rules.py` (alongside the other shared canonical lists); all 4 consumers import from there. The MEMORY note "Extensibility first" explicitly calls this out as the anti-pattern to avoid |
| AU-X1-002 | P1 | scripts/podcast/orchestrate_book.py:865-1339 + _progress.py:112 | Phase-status enum `{running, completed, failed, halted, pending, ship-with-caution}` — 6 string literals scattered across ~25 call sites with no central definition or type-check | Define `PhaseStatus = Literal[...]` in `_progress.py`; refactor all `update_phase(..., status="...")` callers to import the constant; mypy/pyright would then catch typos |
| AU-X2-001 | P0 | infra/claude-agents/{podcast-challenger.md, slide-deck-challenger.md, podcast-auditor.md} | Three "challenger-shaped" agents (challenger + slide-deck-challenger + auditor) authored independently with overlapping `challenger_contract` / `auditor_contract` frontmatter blocks. No `BaseChallenger` reference doc, no `_rules.py::emit_finding` is the only shared substrate. As the auditor spec acknowledges ("podcast-challenger and slide-deck-challenger use" the substrate), the pattern is real — but there's no scaffold for the next one | Author `reference/cortex-challenger-framework.md` (the podcast-challenger spec already references this path at line 66 but the file does not exist on disk — corroborates AU-A1) as the formal base contract. Or, less ambitiously, extract the common frontmatter shape into a YAML schema fragment that all three include |
| AU-X2-002 | P0 | scripts/podcast/sync-agent-wrappers.sh + infra/claude-agents/* + .github/agents/* | The `sync-agent-wrappers.sh` script enforces an invariant ("infra/claude-agents/*.md byte-identical to .github/agents/*.agent.md") that is currently BROKEN: (a) `infra/claude-agents/podcast-blueprint.md` and `.github/agents/podcast-blueprint.agent.md` differ; (b) `infra/claude-agents/refine-prompt.md` and `.github/agents/refine-prompt.agent.md` differ; (c) `podcast-auditor.md`, `podcast-challenger.md`, `podcast-extract.md`, `slide-deck-challenger.md` exist in `infra/claude-agents/` with NO `.github/agents/` counterpart; (d) `.claude/agents/podcast-challenger.md` (May 22) is stale vs `infra/claude-agents/podcast-challenger.md` (May 23) | (1) Run `./scripts/podcast/sync-agent-wrappers.sh --check` and observe it fails; (2) decide which direction is canonical — the script says `.github/agents/` is canonical but the recent additions all land in `infra/claude-agents/` first; (3) update the script to handle the four new agents (or back-fill the `.github/agents/*.agent.md` files); (4) wire `--check` into CI |
| AU-X3-001 | P2 | infra/claude-agents/podcast-auditor.md (this spec, 2026-05-23) vs older specs | New `auditor_contract` frontmatter block uses a different key shape than the older `challenger_contract` — `auditor_contract.auditor_version` vs the audio challenger which has no version key in frontmatter at all. The slide-deck-challenger uses `challenger_version` (lowercase) in its sample JSON | Standardize on one shape: `<agent>_contract.<agent>_version` everywhere; update older specs to add the version key for symmetry |
| AU-X3-002 | P2 | scripts/podcast/phases/ vs scripts/podcast/*.py top-level | Phase runner modules use `pN_M.py` (snake_case with underscore between phase + step: `p1_1.py`, `p4_3.py`, `p25_7.py`) but top-level scripts use `build_episode_txt.py`-style snake_case. Inconsistent: should phase runners follow the same `verb_noun.py` convention (e.g., `phase_1_1_refine_source.py`)? | Document the phase-runner naming convention in `scripts/podcast/phases/__init__.py` or accept the divergence with a CONVENTIONS.md note. Low impact; flagged for awareness |

## Detail

### AU-A1-001 — Mass spec drift in plan/yaml docs
- Citation: [_workspace/plan/podcast-plan.yaml](_workspace/plan/podcast-plan.yaml), [_workspace/plan/acceptance-criteria.md](_workspace/plan/acceptance-criteria.md), [_workspace/plan/research/2026-05-19-redesign-proposal-from-air.md](_workspace/plan/research/2026-05-19-redesign-proposal-from-air.md)
- VERIFIED: 22+ path references that don't exist on disk (full list in table)
- Why it matters: These planning docs were written before the 2026-05-19 redesign and the 2026-05-22 repo split. Anyone reading them today follows broken trails to non-existent scripts (`_blueprint.py`, `_content_range.py`, `_isolation_check.py`, etc.). High blast radius — these are the primary planning surface.
- Proposed fix: Add a YAML/markdown header `[ARCHIVED 2026-05-19 — references pre-redesign paths; see CLAUDE.md for current pipeline]` OR systematically excise dead references. Recommended: archive header (preserves historical context cheaper than rewriting).
- safe_to_auto_fix: false (requires editorial judgment about which references to rescue vs archive)

### AU-A1-002 — podcast-operator references wrong path for coordination-protocol
- Citation: [infra/claude-agents/podcast-operator.md:128](infra/claude-agents/podcast-operator.md), [infra/claude-agents/podcast-operator.md:143](infra/claude-agents/podcast-operator.md)
- VERIFIED: `grep -n` confirms two lines; `ls _workspace/plan/coordination-protocol.md` → no such file; canonical is `_workspace/plan/operators/coordination-protocol.md` (confirmed exists)
- Why it matters: The operator agent runs on every session-start. A path miss here breaks Asif's first-touch diagnostic.
- Proposed fix: `sed -i '' 's|_workspace/plan/coordination-protocol.md|_workspace/plan/operators/coordination-protocol.md|g' infra/claude-agents/podcast-operator.md`
- safe_to_auto_fix: true (mechanical path correction)

### AU-A1-003 — podcast-challenger changelog references retired registry path
- Citation: [infra/claude-agents/podcast-challenger.md:615](infra/claude-agents/podcast-challenger.md)
- VERIFIED: The changelog text says "`content/podcast/_handbook/registry.md` → `content/podcast/.skill/registry.md`" — but `content/podcast/.skill/registry.md` does NOT exist (only `content/podcast/.skill/handbook/` exists). The actual registries are at `content/drafts/<slug>/_system/registry.md`.
- Why it matters: Documents the wrong target of the historical move. A new contributor reading the changelog would look for a non-existent file.
- Proposed fix: Annotate the changelog entry with `(historical — registries now live per-book at content/drafts/<slug>/_system/registry.md)`
- safe_to_auto_fix: false (requires understanding of the workspace-restructure narrative)

### AU-A1-004 — Handoff doc references pre-Phase-9.5 library paths
- Citation: [_workspace/plan/handoff-kar-archetype-pivot.md](_workspace/plan/handoff-kar-archetype-pivot.md)
- VERIFIED: References `content/drafts/kitab-al-riyad/_system/orchestrator-state.json` and `…/EP10-motion-stillness-hyle-and-form/00-framing.md` — the actual state file is at `content/drafts/kitab-al-riyad/_system/orchestrator-state.json` per current convention.
- Why it matters: Handoff docs are read at literal moments of cross-machine context transfer. Wrong path = wasted time at the worst possible moment.
- Proposed fix: Path replacement throughout the handoff doc; verify all `content/drafts/…` refs.
- safe_to_auto_fix: false (some refs may be intentional pre-Phase-9.5 history)

### AU-S2-001 — Hardcoded user path in podcast-extract agent
- Citation: [infra/claude-agents/podcast-extract.md:43](infra/claude-agents/podcast-extract.md)
- VERIFIED: literal `/Users/asifhussain/PROJECTS/podcast-factory/worktrees/main/scripts/podcast/extract_chapter.py`
- Why it matters: This agent is intended to run cross-machine (Studio + Air per CLAUDE.md). Absolute path bakes in one machine's directory layout.
- Proposed fix: Replace with `scripts/podcast/extract_chapter.py` (relative). The agent runs in repo-root cwd per convention.
- safe_to_auto_fix: true (pure path rewrite)

### AU-S2-002 — Hardcoded user paths in refine-prompt agent
- Citation: [infra/claude-agents/refine-prompt.md:28-34](infra/claude-agents/refine-prompt.md)
- VERIFIED: 6 absolute path references in orientation block
- Why it matters: Same as AU-S2-001, but in the orientation block — every invocation reads these.
- Proposed fix: Replace all six with relative paths (`scripts/podcast/`, `.github/agents/`, etc.)
- safe_to_auto_fix: true

### AU-S3-001 — Slide-deck path has zero cost-ledger emissions
- Citation: [scripts/podcast/build_slide_deck.py](scripts/podcast/build_slide_deck.py), [scripts/podcast/_slide_authoring.py](scripts/podcast/_slide_authoring.py), [scripts/podcast/_slide_convergence.py](scripts/podcast/_slide_convergence.py), [content/drafts/kitab-al-riyad/_system/orchestrator-state.json](content/drafts/kitab-al-riyad/_system/orchestrator-state.json)
- VERIFIED: `grep -n cost` across the three modules returns one comment-only hit. The KaR `orchestrator-state.json` `cost` dict has only `azure_usd` and `anthropic_usd` keys. The 0g phase has run on KaR but produced 0 cost rows.
- Why it matters: The $50/chapter cost cap is the only guard against runaway LLM spending in the autonomous orchestrator path. Slide-deck convergence can spawn the slide-deck-challenger (up to 5 inner iterations × N chapters) — that is real Anthropic spend invisible to the cap calculation.
- Proposed fix: Mirror the audio path's `append_cost_row` calls in `_slide_convergence.run_slide_convergence` and the slide-authoring step. Add `slide_deck_usd` to `_progress.py:_initial_state` cost dict. ~30-line diff.
- safe_to_auto_fix: false (correctness-sensitive; requires verifying which calls produce real spend)

### AU-X2-001 — No formal base-challenger contract; referenced doc doesn't exist
- Citation: [infra/claude-agents/podcast-challenger.md:66](infra/claude-agents/podcast-challenger.md)
- VERIFIED: References `reference/cortex-challenger-framework.md` as "the shared contract every challenger satisfies" — this file does not exist on disk under `reference/` or anywhere in the repo.
- Why it matters: The system is accruing challenger-shaped agents (audio, slide-deck, auditor) faster than the framework can absorb them. Without a base contract, the next challenger (likely a `podcast-trainer-challenger` or a reader-side validator) will diverge.
- Proposed fix: Either author `reference/cortex-challenger-framework.md` to formalize the shared contract, or remove the broken reference and document the de-facto contract inline in each challenger spec.
- safe_to_auto_fix: false (requires design decision)

### AU-X2-002 — Broken agent-spec mirror invariant
- Citation: [scripts/podcast/sync-agent-wrappers.sh](scripts/podcast/sync-agent-wrappers.sh), [infra/claude-agents/](infra/claude-agents/), [.github/agents/](.github/agents/), [.claude/agents/](.claude/agents/)
- VERIFIED: 4 agents missing from `.github/agents/` (`podcast-auditor`, `podcast-challenger`, `podcast-extract`, `slide-deck-challenger`); 2 pairs diverged (`podcast-blueprint`, `refine-prompt`); `.claude/agents/podcast-challenger.md` mtime 2026-05-22 vs canonical mtime 2026-05-23
- Why it matters: The sync-wrapper script's docstring explicitly says "if the canonical spec edits without a wrapper sync, the activated agent runs on stale text" — that failure mode is now realized. Asif's agents in `.claude/agents/` may be running stale specs.
- Proposed fix: (a) decide canonical direction (currently `.github/agents/` per the script, but new work lands in `infra/claude-agents/`); (b) back-fill or remove the script; (c) wire `--check` into a pre-commit hook.
- safe_to_auto_fix: false (requires design decision about canonical location)

### AU-X1-001 — ALLOWED_CATEGORIES enum quadrupled
- Citation: [scripts/podcast/orchestrate_book.py:106](scripts/podcast/orchestrate_book.py), [scripts/podcast/scaffold_book.py:39](scripts/podcast/scaffold_book.py), [scripts/podcast/ingest_source.py:71](scripts/podcast/ingest_source.py), [scripts/podcast/audit_page_markers.py:222](scripts/podcast/audit_page_markers.py)
- VERIFIED: 4 separate definitions of the same 6-element list `{books, articles, documents, lectures, interviews, letters}`, with inconsistent type (tuple vs set vs inline list).
- Why it matters: Adding a new category requires touching 4 files; high probability of drift. The MEMORY note "Extensibility first" calls this out as the exact anti-pattern Asif is trying to avoid.
- Proposed fix: Define once in `_rules.py:ALLOWED_CATEGORIES`; all 4 consumers `from _rules import ALLOWED_CATEGORIES`. ~10-line patch.
- safe_to_auto_fix: true (pure refactor; behavior-preserving)

### AU-X1-002 — Phase-status enum unstructured
- Citation: [scripts/podcast/orchestrate_book.py:865-1339](scripts/podcast/orchestrate_book.py), [scripts/podcast/_progress.py:112](scripts/podcast/_progress.py)
- VERIFIED: 6 distinct status strings (`running`, `completed`, `failed`, `halted`, `pending`, `ship-with-caution`) appear as inline string literals in ~25+ `update_phase(..., status="...")` calls. No `Literal[...]` type; no central definition.
- Why it matters: Typos like `"complete"` vs `"completed"` would not be caught at edit-time. The status drives orchestrator resume logic (the resume bug in MEMORY directly depends on these values).
- Proposed fix: `PhaseStatus = Literal["running", "completed", "failed", "halted", "pending", "ship-with-caution"]` in `_progress.py`; consumers import.
- safe_to_auto_fix: true (mechanical)

### (other Axis A/B/C/D findings as enumerated in tables above)

## Cross-cohort patterns

**Pattern P-1: Pre-2026-05-22 split documentation drift.** AU-A1-001 (acceptance-criteria.md, podcast-plan.yaml), AU-A1-003 (challenger changelog), AU-A1-004 (handoff doc) all reference paths from before the repo split + Phase-9.5 hoist. The system migrated; the docs didn't. High-leverage fix: a single audit-and-archive sweep of `_workspace/plan/*.md` for pre-2026-05-22 path patterns (`content/drafts/<slug>/_system/`, retired `_blueprint.py` family).

**Pattern P-2: Agent-spec sprawl without a contract.** AU-X2-001 (missing `cortex-challenger-framework.md`) + AU-X2-002 (broken mirror) + AU-X3-001 (frontmatter shape divergence) all point at the same root: the agent-spec layer has no governance. Three locations (`infra/`, `.github/`, `.claude/`), inconsistent frontmatter shapes (`challenger_contract` vs `auditor_contract` vs none), no base contract doc. High-leverage fix: establish ONE canonical location, write ONE base spec, deprecate the others.

**Pattern P-3: Shared constants not yet shared.** AU-X1-001 (categories), AU-X1-002 (phase statuses), AU-S1-001 (word-count bands), AU-S1-002 (iteration caps), AU-S1-003 (cost cap) — all are de-facto-shared constants that physically live in one file and are re-typed elsewhere. High-leverage fix: introduce `scripts/podcast/_config.py` as the central knobs module; relocate the listed constants; update consumers via from-import.

## What's next

Ranked by impact × effort, lowest effort first:

1. **Fix AU-A1-002 + AU-S2-001 + AU-S2-002.** Six path corrections in three agent specs. ~5 minutes of mechanical `sed`. High value because the specs are read on every invocation. (safe_to_auto_fix in v1.1)
2. **Fix AU-X1-001 (ALLOWED_CATEGORIES centralization).** ~30-minute refactor; eliminates a documented anti-pattern; ratchets the codebase one step toward the "Extensibility first" MEMORY directive.
3. **Decide AU-X2-002 (agent-spec sync canonical direction).** This is a 15-minute design decision + sync-script update. Without it, every new agent silently amplifies the mirror drift.
4. **Plan AU-A1-001 (pre-split plan-doc archive).** Half-day sweep. Decide per-file: archive header vs surgical edits. Highest documentation-leverage.
5. **Plan AU-S3-001 (slide-deck cost-ledger integration).** ~1 hour. Real correctness gap; without it the $50 cap is silently bypassed on every 0g run.

P0 count = 6 → verdict `regression-detected` per spec verdict logic. Recommend addressing items 1-3 before next book is queued.
