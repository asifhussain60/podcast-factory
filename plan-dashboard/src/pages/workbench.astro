---
// /workbench — retired surface. The operator cockpit is now the "Edit & Enrich"
// step of the sequential Studio pipeline. Redirect to the Studio book picker so
// the user selects a book first (no more hardcoded single book).
return Astro.redirect('/studio');
---
