export interface PlanVisualManifest {
  preWaveTerm: string;
}

// Central display labels for plan UI rendering.
export const planVisualManifest: PlanVisualManifest = {
  preWaveTerm: 'Mobilization',
};
