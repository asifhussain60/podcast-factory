# azure-config.env — LIVE config for the journal app's Azure stack.
#
# This file matches what's actually deployed in rg-journal-ai as of 2026-05-18.
# Values here are NOT secrets — they're resource names, region, and the
# subscription ID (which is also visible in every Azure portal URL). Safe to
# commit.
#
# If you're adapting this for a different app: cp azure-config.template.env
# to a new name (e.g. triplog-config.env) and edit there. Don't overwrite
# this file unless you're actually changing what's deployed.

# ----------------------------------------------------------------------------
# Identity
# ----------------------------------------------------------------------------
APP_NAME="podcast-factory"
APP_DESCRIPTION="Podcast pipeline — Arabic OCR + English translation (label-only rename 2026-05-22 in repo split; actual Azure resources retain journal-* names — see docs/runbooks/ repo-split history §2 decision 3)"
ALERT_EMAIL="asifhussain60@gmail.com"

# ----------------------------------------------------------------------------
# Azure placement
# ----------------------------------------------------------------------------
SUBSCRIPTION_ID="3440564d-c056-4173-bec6-7af92dbece77"
RESOURCE_GROUP="rg-journal-ai"
LOCATION="eastus"

# ----------------------------------------------------------------------------
# Feature flags
# ----------------------------------------------------------------------------
ENABLE_TRANSLATOR="true"
ENABLE_DOCINTEL="true"
ENABLE_SPEECH="true"      # Enabled 2026-05-18 for Urdu-lecture transcription (kunooz-al-hikmah)
ENABLE_STORAGE="true"
ENABLE_KEYVAULT="true"    # Enabled 2026-06-02: kv-journal-ai provisioned for multi-Mac portability
ENABLE_LANGUAGE="true"   # journal-language-market — TextAnalytics (NER, key-phrase, sentiment) — wired 2026-06-06
ENABLE_OPENAI="true"     # journal-openai — Azure OpenAI resource provisioned 2026-06-06.
                         # DALL-E 3 v3.0 deprecated Mar 2026; gpt-image-1 not yet in eastus.
                         # whisper v001 is GA — deployable for transcription if needed.
                         # Image gen stays on Gemini until Azure lands a replacement image model.

# ----------------------------------------------------------------------------
# Translator
# ----------------------------------------------------------------------------
TRANSLATOR_NAME="journal-translator"
TRANSLATOR_SKU="S1"

# ----------------------------------------------------------------------------
# Document Intelligence
# ----------------------------------------------------------------------------
DOCINTEL_NAME="journal-docintel"
DOCINTEL_SKU="S0"   # Upgraded from F0 on 2026-05-18: F0 has 4MB binary cap that blocks Kitab-al-Riyad.pdf (4.4 MB). S0 cap is 500MB.

# ----------------------------------------------------------------------------
# Speech — Fast Transcription (post-publication transcript for Loop M)
# ----------------------------------------------------------------------------
SPEECH_NAME="journal-speech"
SPEECH_SKU="S0"     # F0 = 5 audio-hours/month free; S0 = $1/audio-hour pay-as-you-go.

# ----------------------------------------------------------------------------
# Storage
# ----------------------------------------------------------------------------
STORAGE_ACCOUNT_NAME="journalpodcaststorage"
STORAGE_SKU="Standard_LRS"
STORAGE_CONTAINERS="source-arabic source-urdu translated-english"

# ----------------------------------------------------------------------------
# Key Vault — ACTIVE, and the pipeline's only credential source at runtime.
# scripts/podcast/_secrets.py and _azure_creds.py read env var -> this vault.
# The macOS-keychain tier was removed 2026-06-04; see infra/pipeline-runtime.md.
# ----------------------------------------------------------------------------
KEYVAULT_NAME="podcast-factory-vault"

# ----------------------------------------------------------------------------
# Budget (set up manually in portal — see docs/azure/setup.md § Step 4)
# ----------------------------------------------------------------------------
# ----------------------------------------------------------------------------
# Language (TextAnalytics — NER, key-phrase extraction, sentiment)
# ----------------------------------------------------------------------------
LANGUAGE_NAME="journal-language-market"
LANGUAGE_SKU="F0"   # F0 = 5,000 text records/month free. Upgrade to S if augmentation exceeds quota.

# ----------------------------------------------------------------------------
# Azure OpenAI (DALL-E 3 image generation)
# ----------------------------------------------------------------------------
OPENAI_NAME="journal-openai"
OPENAI_SKU="S0"
OPENAI_DALLE_DEPLOYMENT="dall-e-3"
OPENAI_DALLE_VERSION="3.0"

# ----------------------------------------------------------------------------
# Budget (set up manually in portal — see docs/azure/setup.md § Step 4)
# ----------------------------------------------------------------------------
BUDGET_NAME="journal-ai-monthly-cap"
BUDGET_AMOUNT_USD="50"
BUDGET_THRESHOLDS="50 80 100"
